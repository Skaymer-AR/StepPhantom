# StepPhantom build-fixed

Rama temporal para compilar `StepPhantom_fixed.zip` sin modificar `main`.

El workflow `Build fixed StepPhantom APK` extrae el proyecto corregido y publica el artifact `StepPhantom-fixed-debug-apk`.
